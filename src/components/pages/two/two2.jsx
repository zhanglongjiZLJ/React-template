import React, { Component } from 'react';import { Tag ,Button} from 'antd';import { createStore } from 'redux';import {  Link} from 'react-router-dom'class App extends Component {  constructor(props) {    super(props);    this.state = {      tagMenu:[        {          name:'首页',          path:'/about/home'        },        {          name:'设置',          path:'/about/set'        },        {          name:'信息',          path:'/about/info'        }      ]    };    this.handleClick = this.handleClick.bind(this);  }  handleClick() {    this.state.tagMenu.push({      name:'呵呵',      path:'/about/hehe'    })    console.log(this.state.tagMenu);    this.setState(prevState => ({      tagMenu: this.state.tagMenu    }));  }  render() {    return (      <div>          {            this.state.tagMenu.map(function(item){              return <Tag closable key={item.path} ><Link to={item.path}>{item.name}</Link></Tag>            })          }          <Button onClick={this.handleClick} type="primary">Primary</Button>    </div>    )  }}export default App;